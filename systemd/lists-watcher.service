[Unit]
Description=Lists restarter
After=network.target

[Service]
Type=oneshot
ExecStart=/bin/sh -c 'cargo build && systemctl restart lists.service'
User=root
WorkingDirectory=/root/lists

[Install]
WantedBy=multi-user.target
